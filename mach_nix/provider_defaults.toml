_default =                "wheel,sdist,nixpkgs"
setuptools =              "nixpkgs"
wheel =                   "nixpkgs,sdist"
setupmeta =               "wheel"


# gets mapped to 'importmagic' in nixpkgs provider which is the wrong package
importmagic3 =            "sdist,wheel"
