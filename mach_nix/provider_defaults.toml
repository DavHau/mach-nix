#_default =                "wheel,sdist,nixpkgs"
_default =                "conda"

gdal =                    "nixpkgs"
pip =                     "nixpkgs,sdist"
setupmeta =               "wheel"
setuptools =              "nixpkgs"
wheel =                   "nixpkgs,sdist"